export enum Routes {
  HOME = "/",
  REGISTER = "/register",
  LOGIN = "/login",
  STATUS = "/status",
  HEXMAPS = "/hexmaps",
  CHARACTERS = "/characters",
  LOBBIES = "/lobbies",
  USER = "/user",
  LOBBY = "/lobby/:id",
  GAME = "/game/:id",
}
