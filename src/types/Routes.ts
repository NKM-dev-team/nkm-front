export enum Routes {
  HOME = "/",
  STATUS = "/status",
  HEXMAPS = "/hexmaps",
  CHARACTERS = "/characters",
  LOBBIES = "/lobbies",
  // USER = "/user",
  LOBBY = "/lobby/:id",
  GAME = "/game/:id",
}
