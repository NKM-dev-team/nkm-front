export enum Routes {
  HOME = "/",
  STATUS = "/status",
  HEXMAPS = "/hexmaps",
  CHARACTERS = "/characters",
  LOBBIES = "/lobbies",
  ADMIN = "/admin",
  RELEASE = "/release",
  // USER = "/user",
  LOBBY = "/lobby/:id",
  GAME = "/game/:id",
}
