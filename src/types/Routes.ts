export enum Routes {
  HOME = "/",
  REGISTER = "/register",
  LOGIN = "/login",
  HEXMAPS = "/hexmaps",
  CHARACTERS = "/characters",
  LOBBIES = "/lobbies",
  USER = "/user",
  LOBBY = "/lobby/:id",
}
