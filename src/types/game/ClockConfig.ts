export interface ClockConfig {
  initialTimeMillis: number;
  incrementMillis: number;
  maxBanTimeMillis: number;
  maxPickTimeMillis: number;
  timeAfterPickMillis: number;
  timeForCharacterPlacing: number;
}